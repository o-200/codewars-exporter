# frozen_string_literal: false

system('bundle')

puts 'input your nickname from codewars:'
nick = gets.chomp.to_s

file = File.new('.codewars-nick', 'w+')
file.print(nick)
file.close

puts "'#{File.basename(file)}' was created!"
